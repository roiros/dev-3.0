Second UI pass: switched from ultra-dark (#0f1014) to midnight indigo (#171924) palette. Bumped all font sizes up (titles text-base/text-lg, card text text-sm, labels text-sm). Wider Kanban columns (240px). Cards now have colored left border accent (3px). More vibrant status colors. Bigger padding and spacing throughout. More rounded corners (rounded-2xl). Button shadows for depth.
