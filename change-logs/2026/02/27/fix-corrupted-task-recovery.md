Graceful handling of corrupted tasks whose worktree directory or branch was deleted externally. Backend now checks for worktree existence before spawning PTY processes or running cleanup scripts, and wraps removeWorktree/cleanup in try-catch so moveTask always completes. Frontend shows a recovery popup with Complete/Cancel buttons instead of an infinite "Connecting..." spinner when the PTY session fails.
