Extracted shared `activateTask()` helper in rpc-handlers.ts to deduplicate worktree creation + CoW clones + PTY launch logic. This fixes a bug where the CLI `task.move` handler was missing `runCowClones` and `existingBranch` support when resuming tasks from completed/cancelled status. All four call sites (moveTask, createTask, moveTaskToCustomColumn in rpc-handlers, and task.move in cli-socket-server) now use the shared helper.
