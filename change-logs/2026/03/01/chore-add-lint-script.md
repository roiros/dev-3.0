Added `bun run lint` script that runs TypeScript type-checking via `tsc --noEmit`, filtering errors to `src/` only (electrobun ships raw `.ts` sources, not `.d.ts`, so `skipLibCheck` does not suppress its issues). Fixed two pre-existing type errors in `pty-server.ts` (`proc.stdin` cast and implicit-any callback params). Updated `AGENTS.md` to require `bun run lint` alongside `bun run test` before every commit.
