Rewrite Codex sandbox integration with dedicated `[permissions.dev3]` and `[profiles.dev3]` profiles using absolute paths. Fix CLI context detection when sandbox rewrites HOME=/tmp. Add shell="/bin/bash" hint to dev3 skill description and AGENTS.md for Codex agents (env-level SHELL doesn't work). Restructure Codex presets: non-bypass use `-a on-request` (not `--full-auto` which bypasses permission profiles), bypass presets keep `--sandbox danger-full-access`.
