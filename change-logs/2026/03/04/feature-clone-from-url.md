Add "Clone from URL" option when adding a new project. Users can now paste a git URL and the app will clone the repository automatically. The clone base directory is remembered in global settings for future use. The Add Project button now opens a modal with two tabs: "Local Folder" (existing flow) and "Clone from URL" (new). The clone directory can also be configured in Global Settings.
