Add CLI commands for label management: `label list`, `label create`, `label delete`, `label set`, and `label set --clear`. Also add `--label` filter to `tasks list`. Corresponding CLI socket handlers added to the backend. Agent skill updated with label documentation. Fixed short label ID prefix resolution so CLI-set labels display correctly in UI. Labels now shown in TaskInfoPanel metadata grid. Added `projectUpdated` push message so label create/delete via CLI instantly updates the UI without manual refresh.
