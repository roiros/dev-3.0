Refactored TaskInfoPanel header from a single crowded row into two rows. Top row: task status (leftmost), label chips, spacer, tmux keyboard hints (⌃B -, |, z + ⓘ popover), dev server, fullscreen, collapse. Bottom row: git info full width — branch, ahead/behind with push/merge/rebase buttons, uncommitted changes. Collapsed panel height increased from 36px to 62px. In split view (ProjectView), TaskInfoPanel is now rendered above the SplitLayout spanning full window width instead of being confined to the narrow terminal pane. TaskTerminal gained a `hideInfoPanel` prop to suppress the duplicate panel in split mode.
