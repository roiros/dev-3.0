Added version-based preset update mechanism for agent configurations. When a default preset's version is bumped, stored `additionalArgs` are reset to the new defaults while preserving user-editable fields (model, permissionMode, effort, etc.). All Codex presets set to version 2, which forces a one-time reset of stale cached args (e.g. old `--full-auto` flags) to the current defaults.
