Auto-allow dev3 CLI commands in Claude Code. The app now injects `Bash(~/.dev3.0/bin/dev3 *)` into `~/.claude/settings.json` permissions on startup, so Claude Code never prompts for approval on dev3 commands. Split agent skills into Claude-specific (with `!`command`` injection for zero tool calls at startup — `--help`, `current`, and auto `in-progress` status) and generic (for Cursor, Codex, Gemini, etc. — unchanged behavior with explicit bash calls).
