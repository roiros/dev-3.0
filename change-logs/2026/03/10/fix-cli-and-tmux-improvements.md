Add --if-status-not CLI flag with comma-separated support for task move guards. Fix tmux -L dev3 missing from copy-to-clipboard command in session manager. Enforce tmux socket as non-nullable string with DEFAULT_TMUX_SOCKET constant, removing null-socket code paths. Fix worktree path escaping in setup script split command. Remove leftover console.log debug statements from data layer. Simplify agent prompts to delegate status management entirely to hooks.
