Extracted `escapeForDoubleQuotes`, `buildEchoAndRun`, and `buildCmdScript` as testable functions from `launchTaskPty()` in `rpc-handlers.ts`. Fixed the echo display wrapper to properly escape all double-quote-sensitive shell metacharacters (`"`, `$`, `` ` ``, `\`, `!`) instead of only double quotes. Added comprehensive unit tests covering shell escaping for task creation: `shellEscape` (20+ edge cases), `resolveAgentCommand` with special characters, `escapeForDoubleQuotes`, `buildEchoAndRun`, `buildCmdScript`, and end-to-end pipeline tests including injection attempts, unicode, and mixed special chars.
