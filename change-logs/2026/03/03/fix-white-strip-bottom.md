Remove spurious `padding-bottom: 16px` from `body, #root` in index.css that caused a white strip at the bottom of the window. Also move the gradient background from `#root`-only to the shared `body, #root` rule so the body background matches the app theme. Set the terminal container background to match the terminal theme color, hiding the ~16px right-side gap caused by ghostty-web FitAddon's hardcoded scrollbar reservation.
