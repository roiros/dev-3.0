Reinforced task status management requirements in both the `--append-system-prompt` injection and the dev3 skill template. The system prompt now explicitly lists the three required status transitions (`in-progress`, `user-questions`, `review-by-user`) with clear instructions, instead of only telling agents to load the skill. The skill text was also expanded with stricter rules and phase-by-phase guidance to ensure agents never skip status updates.
