Fixed 8 bugs in the dev3 CLI: added --id flag support for task show/update/move; added --description flag to task create; added client-side status validation for task move and tasks list; added --limit support for tasks list; added short ID resolution (8-char IDs from `tasks list` now expand to full UUIDs via context prefix match and data file fallback); added whitespace validation for task create/update --title/--description and note add content; added --limit numeric validation rejecting NaN, negative, and partial numbers.
